string c=aux->priori;
d=atoi(c.c_str());
if(d==1){
	string e=aux->dato4;
	f=atoi(e.c_str());
	string g=aux->dato3;
	h=atoi(g.c_str());
	n=atoi(g.c_str());
	
	for(int q=0;q<=h;q++){
		if(q<f){
			n+=1;
		}else{
			m++;
		}
		
		k=q;
		
	}
	l++;
	
}

if(d==2){
	string e=aux->dato4;
	f=atoi(e.c_str());
	string g=aux->dato3;
	h=atoi(g.c_str());
	n=atoi(g.c_str());
	
	for(int q=0;q<=h;q++){
		if(q<f){
			n+=1;
		}else{
			m++;
		}
		
		k=q;
		
	}
	l++;
	
}else{
	string e=aux->dato4;
	f=atoi(e.c_str());
	string g=aux->dato3;
	h=atoi(g.c_str());
	n=atoi(g.c_str());
	
	for(int q=0;q<=h;q++){
		if(q<f){
			n+=1;
		}else{
			m++;
		}
		
		k=q;
		
	}
	l++;	
	
}
